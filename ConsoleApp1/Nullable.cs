using System;
using System.Collections.Generic;
using System.Text;

namespace ConsoleApp1
{
    class Nullable
    {
        //static void Main(string[] args)
        //{
        //    int? x = null;
        //    double? y = 10.0;

        //    Console.WriteLine(x.GetValueOrDefault()); // vai trazer o valor defaul do tipo int que no caso é zero
        //    Console.WriteLine(y.GetValueOrDefault()); // vai trazer o valor 10.0

        //    Console.WriteLine(x.HasValue); // retorna false
        //    Console.WriteLine(y.HasValue); // retorna true

        //    Console.WriteLine(x.Value); // lança exception, tratar para nao quebrar o programa

        //    double? w = null;
        //    double z = w ?? 0.0; //se o valor for nulo, vai atribuir 0.0, senao atribui o valor da variavel


            

        //}
    }
}
